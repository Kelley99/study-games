## 任务背景
用户重新测试后发现三个OCR字段全部取值错误。

## 执行过程
1. 同盟："凌霄"被识别为"战争"——匹配逻辑有误
2. 集结加成：取了上方的59.56%而非31.01%——代码仍在找含+号的行
3. 出征容量：159111 vs 159110——OCR识别差一位
4. 需要看OCR原始返回文本才能写准正则

## 关键结果
- 三个字段取值逻辑全部需修正
- 等待用户提供/api/ocr返回的原始JSON
- 无法仅凭截图写出准确正则

## 结论建议
用户提供图2的OCR原始返回文本后，可根据实际格式精确修正三个字段的提取逻辑。